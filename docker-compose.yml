services:
  postgres:
    image: pgvector/pgvector:0.8.3-pg17-trixie
    environment:
      POSTGRES_USER: jobhunter
      POSTGRES_PASSWORD: jobhunter_dev
      POSTGRES_DB: jobhunter
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jobhunter -d jobhunter"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      APP_MODE: local
      DEMO_SEED_ON_STARTUP: "false"
      DATABASE_URL: postgresql+psycopg://jobhunter:jobhunter_dev@postgres:5432/jobhunter
      EMBEDDING_PROVIDER: ollama
      EMBEDDING_MODEL: nomic-embed-text
      GENERATION_PROVIDER: ollama
      GENERATION_MODEL: qwen3:4b
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      EMBEDDING_TIMEOUT_SECONDS: "60"
      GENERATION_TIMEOUT_SECONDS: "120"
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    environment:
      API_BASE_URL: http://backend:8000
      FRONTEND_REQUEST_TIMEOUT_SECONDS: "120"
    ports:
      - "8501:8501"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
    name: ai_job_hunter_postgres_data
